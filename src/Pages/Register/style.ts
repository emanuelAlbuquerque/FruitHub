import styled from "styled-components";
import { ContainerPages } from "styles/ContainerPages";

export const ContainerStylePageRegister = styled(ContainerPages)`

  text-align: center;

  .img_logo{
    margin: 2rem 0 2rem 0;
  }

  .button{
    width: 90%;
    margin: 3rem auto 0 auto;
  }
`